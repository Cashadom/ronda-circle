'use client'

import { useEffect, useState } from 'react'
import { useParams } from 'next/navigation'
// ... tout le reste de ton code actuel

export default function CirclePageClient() {
  const { id } = useParams()
  // ... tout ton code existant
}